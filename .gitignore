.DS_Store
.playwright-mcp/

# Secrets / real values — never commit to this public repo
.env
*.env
**/.env
litellm-docker/.env
CLAUDE.md
*.docx
# Local manifests with real ARNs/account id/domain (not sanitized — keep local only)
13-firecrawl.yaml
14-ingress-direct.yaml
# Docker (Tokyo EC2) deployment kit — kept local; contains real .env / config, not for this public repo
litellm-docker/
# Real render values (account id, domain, cert/waf arns) — local only
values.local.env
# but DO commit the placeholder example
!values.example.env

# AgentCore runbook 真实值清单(本地)
docs/agentcore-runbook.values.local

# AgentCore runbook 完整真实值版(含账号/域名/gateway id) — 本地参考,绝不入库
docs/agentcore-websearch-runbook.REAL.*

# runbook 只入库 md;docx/html 本地生成,不入库
docs/agentcore-websearch-runbook.html

# 含真实域名,本地 POC 脚本,不入库
docs/ttft-poc-test.py

# Reference: AWS-blog CFN+ECS LiteLLM repo (zhangzhenhuajack/aws-builder), downloaded for analysis
litellm-ecs-cfn-reference/
