.DS_Store
.playwright-mcp/

# Secrets / real values — never commit to this public repo
.env
*.env
**/.env
litellm-docker/.env
CLAUDE.md
*.docx
# Local manifests with real ARNs/account id/domain (not sanitized — keep local only)
13-firecrawl.yaml
14-ingress-direct.yaml
